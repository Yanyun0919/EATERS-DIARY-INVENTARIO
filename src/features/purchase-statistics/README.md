# purchase-statistics

Reporting/analytics over purchasing history (spend by supplier, price trends, variance). Read-only
against Supabase — this is the one area that can assume connectivity rather than being
offline-first. Will own: charts, filters, and the API layer for aggregate queries.

Not implemented yet. Convention when built: `components/`, `hooks/`, `api/`, `pages/`.
