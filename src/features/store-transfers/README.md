# store-transfers

Moving stock between internal locations (send/receive). Will own: transfer request/receipt UI,
local-first draft storage, and the API layer for transfers.

Not implemented yet. Convention when built: `components/`, `hooks/`, `api/`, `schemas/`, `pages/`.
