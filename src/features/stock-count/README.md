# stock-count

Counting stock on hand per store/location. Will own: count session UI, count-line components,
local-first draft storage (RxDB collection), and the API layer that reads/writes counts.

Not implemented yet. Convention when built: `components/`, `hooks/`, `api/`, `schemas/`, `pages/`.
