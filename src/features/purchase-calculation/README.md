# purchase-calculation

Turning stock counts + par levels/usage history into suggested purchase quantities per
supplier. Will own: the calculation engine, review/adjust UI, and the API layer for suggestions.

Not implemented yet. Convention when built: `components/`, `hooks/`, `api/`, `schemas/`, `pages/`.
